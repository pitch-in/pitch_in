<section class="content-block">
  <h2>New Need for <%= @campaign.name %></h2>

  <%= render "form.html", changeset: @changeset,
                          action: campaign_ask_path(@conn, :create, @campaign) %>

  <%= link "Back to Need", to: campaign_ask_path(@conn, :index, @campaign), class: "secondary button" %>
</section>
