<h2>Edit Ask for <%= @campaign.name %></h2>

<%= render "form.html", changeset: @changeset,
                        action: campaign_ask_path(@conn, :update, @campaign, @ask) %>

<%= link "Back", to: campaign_ask_path(@conn, :index, @campaign) %>
