<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width">
    <meta name="keywords" content="progressive,politics,activism">
    <meta name="author" content="Pitch In">
    <title>Pitch In | Page Not Found</title>

    <link rel="shortcut icon" type="image/x-icon" href="<%= webpack_path(@conn, "/favicon.ico") %>" />

    <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,400i" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/foundicons/3.0.0/foundation-icons.css" rel="stylesheet">

    <%= if Mix.env == :prod do %>
      <link href="<%= static_path(@conn, "/main.min.css") %>" rel="stylesheet" type="text/css" />
    <% end %>
  </head>

  <body class="error-page view-login no-js">
    <%= render "_error_navbar.html", conn: @conn %>

    <div class="column row">
      <div class="error-body column">
        <h1 class="text-shadow-dark">404 Page Not Found</h1>

        <img class="error-logo error-logo--404" src="<%= webpack_path(@conn, "/img/logo.svg") %>" />

        <h2 class="text-shadow-dark">Oops!</h2>

        <p>
        It looks like this page has been deleted, moved, or is no longer working. There are still many campaigns and causes looking for someone like you.
        </p>

        <br/>

        <div class="half stacked button-group">
          <a href="/" class="alert dark-shadow button">Home</a>
        </div>
      </div>
    </div>

    <%= render PitchIn.Web.LayoutView, "_footer.html", conn: @conn %>

    <%= if Mix.env == :prod do %>
      <script src="<%= static_path(@conn, "/main.min.js") %>"></script>
    <% else %>
      <script src='http://localhost:4001/main.js'></script>
    <% end %>
  </body>
</html>
